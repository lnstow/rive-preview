{
  "name": "rive-preview",
  "module": "index.html",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot ./index.html",
    "build": "bun build --target=browser ./index.html --outdir=dist && bun -e \"await Promise.all(['robots.txt','sitemap.xml'].map((file) => Bun.write('dist/' + file, Bun.file(file))))\""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@rive-app/webgl2": "^2.37.8",
    "lucide": "^1.16.0"
  }
}
